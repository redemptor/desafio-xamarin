<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TesteXP.Views.MainPage"
             BackgroundColor="#1A202E" NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <Style TargetType="Label">
            <Setter Property="TextColor" Value="White"/>
        </Style>

        <Style x:Key="cellString" TargetType="Label">
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="TextColor" Value="WhiteSmoke"/>
        </Style>

        <Style x:Key="cellNumerica" TargetType="Label" BasedOn="{StaticResource cellString}">
            <Setter Property="HorizontalTextAlignment" Value="End"/>
        </Style>

        <Style x:Key="rowGrid" TargetType="Grid">
            <Setter Property="ColumnDefinitions" Value="2*,*,*,*,40,*,*,*,*,*,*,*,*,*,*"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ColumnSpacing" Value="2"/>
        </Style>

        <Style x:Key="rowTitulo" TargetType="Grid" BasedOn="{StaticResource rowGrid}">
            <Setter Property="Padding">
                <Setter.Value>
                    <OnPlatform x:TypeArguments="Thickness">
                        <On Platform="UWP" Value="5"/>
                        <On Platform="WPF" Value="15,5,25,5"/>
                    </OnPlatform>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="lista" TargetType="ListView">
            <Setter Property="VerticalScrollBarVisibility">
                <Setter.Value>
                    <OnPlatform x:TypeArguments="ScrollBarVisibility">
                        <On Platform="UWP" Value="Default"/>
                        <On Platform="WPF" Value="Always"/>
                    </OnPlatform>
                </Setter.Value>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid RowDefinitions="40,35,*" RowSpacing="1">
            <StackLayout Orientation="Horizontal" Grid.Row="0" BackgroundColor="#293241" Padding="15,0">
                <Label Text="Histórico" VerticalOptions="Center" FontSize="Small"/>
            </StackLayout>

            <Grid ColumnDefinitions="auto,*,auto,auto" Grid.Row="1" Padding="15,0" BackgroundColor="#293241">
                <Label Text="{StaticResource chevron-down}" VerticalOptions="Center" FontSize="12" FontFamily="FontAwesomeSolid" Grid.Column="0"/>
                <Label Text="ORDENS EM ABERTO" VerticalOptions="Center" FontSize="12" Grid.Column="1" />

                <Label Text="{Binding QuantidadeTotal, StringFormat='TOTAL QUANTIDADE: {0}'}" Grid.Column="2" VerticalOptions="Center" FontSize="10" />
                <Label Text="{Binding QuantidadeDisponivelTotal, StringFormat='TOTAL DISPONIVEL: {0}'}" Grid.Column="3" VerticalOptions="Center" FontSize="10" Margin="5,0"/>
            </Grid>

            <ListView Grid.Row="2" ItemsSource="{Binding Ordens}" Style="{StaticResource lista}">
                <ListView.Header>
                    <Grid BackgroundColor="#1A2030" Style="{StaticResource rowTitulo}">
                        <Label Text="DATA E HORA" Grid.Column="0" Style="{StaticResource cellString}"/>
                        <Label Text="ASSESSOR" Grid.Column="1" Style="{StaticResource cellString}"/>
                        <Label Text="CONTA" Grid.Column="2" Style="{StaticResource cellString}"/>
                        <Label Text="ATIVO" Grid.Column="3" Style="{StaticResource cellString}"/>
                        <Label Text="TIPO" Grid.Column="4" Style="{StaticResource cellString}"/>
                        <Label Text="QTD" Grid.Column="5" Style="{StaticResource cellNumerica}"/>
                        <Label Text="QTD. APARENTE" Grid.Column="6" Style="{StaticResource cellNumerica}"/>
                        <Label Text="QTD.DISP" Grid.Column="7" Style="{StaticResource cellNumerica}"/>
                        <Label Text="QTD. CANCEL" Grid.Column="8" Style="{StaticResource cellNumerica}"/>
                        <Label Text="QTD. EXEC" Grid.Column="9" Style="{StaticResource cellNumerica}"/>
                        <Label Text="VALOR" Grid.Column="10" Style="{StaticResource cellNumerica}"/>
                        <Label Text="VALOR DISP" Grid.Column="11" Style="{StaticResource cellNumerica}"/>
                        <Label Text="OBJETIVO" Grid.Column="12" Style="{StaticResource cellNumerica}"/>
                        <Label Text="OBJ.DISP" Grid.Column="13" Style="{StaticResource cellNumerica}"/>
                        <Label Text="REDUÇÃO" Grid.Column="14" Style="{StaticResource cellNumerica}"/>
                    </Grid>
                </ListView.Header>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid BackgroundColor="#232939" Style="{StaticResource rowGrid}">
                                <Label Text="{Binding DataHora}" Grid.Column="0" Style="{StaticResource cellString}"/>
                                <Label Text="{Binding Assessor, TargetNullValue='-'}" Grid.Column="1" Style="{StaticResource cellString}"/>
                                <Label Text="{Binding Conta}" Grid.Column="2" Style="{StaticResource cellString}"/>
                                <Label Text="{Binding Ativo}" Grid.Column="3" Style="{StaticResource cellString}"/>
                                <Label Text="{Binding Tipo, Converter={StaticResource cellTipoConverter}}" Grid.Column="4" Style="{StaticResource cellString}"/>
                                <Label Text="{Binding Quantidade}" Grid.Column="5" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding QuantidadeAparente, Converter={StaticResource cellNumericaConverter}}" Grid.Column="6" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding QuantidadeDisponivel, Converter={StaticResource cellNumericaConverter}}" Grid.Column="7" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding QuantidadeCancelada, Converter={StaticResource cellNumericaConverter}}" Grid.Column="8" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding QuantidadeExecutada, Converter={StaticResource cellNumericaConverter}}" Grid.Column="9" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding Valor, StringFormat='{0:F2}'}" Grid.Column="10" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding ValorDisparo, StringFormat='{0:F2}'}" Grid.Column="11" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding Objetivo, StringFormat='{0:F2}'}" Grid.Column="12" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding ObjetivoDisparo, StringFormat='{0:F2}'}" Grid.Column="13" Style="{StaticResource cellNumerica}"/>
                                <Label Text="{Binding Reducao, Converter={StaticResource cellPercentualConverter}}" Grid.Column="14" Style="{StaticResource cellNumerica}"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </ContentPage.Content>
</ContentPage>